; Compute the final coefficient groups and restore loop output D3.w and XNZVC.
coefficients_tail060:
        move.w $22(a5),d5
        move.w $e(a5),d4
        muls.w d5,d4
        asl.l #1,d4
        swap d4
        move.w d4,$e(a5)
        move.w $12(a5),d4
        muls.w d5,d4
        asl.l #1,d4
        swap d4
        move.w d4,$12(a5)
        move.w $16(a5),d4
        muls.w d5,d4
        asl.l #1,d4
        swap d4
        move.w d4,$16(a5)
        move.w $1a(a5),d4
        muls.w d5,d4
        asl.l #1,d4
        swap d4
        move.w d4,$1a(a5)
        move.w $1e(a5),d4
        muls.w d5,d4
        asl.l #1,d4
        swap d4
        move.w d4,$1e(a5)
        move.w $22(a5),d5
        move.w $3e(a5),d4
        muls.w d5,d4
        asl.l #1,d4
        swap d4
        move.w d4,$3e(a5)
        move.w $42(a5),d4
        muls.w d5,d4
        asl.l #1,d4
        swap d4
        move.w d4,$42(a5)
        move.w #$42,d3
        addq.w #4,d3
        cmpi.w #$42,d3
        jmp $61544.l
